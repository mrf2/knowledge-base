# manim setup
```bash
# Step 1 — System dependencies (the complete list)
sudo apt update
sudo apt install build-essential python3-dev libcairo2-dev libpango1.0-dev

# Step 2 — Install Manim
pip install manim

# Step 3 - Verify
manim --version
```
